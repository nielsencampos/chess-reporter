name: CI

on:
  push:
  pull_request:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Install dependencies
        run: uv sync --frozen

      - name: Run linter
        run: uv run ruff check .

      - name: Run type check
        run: uv run pyright

      - name: Run tests
        run: uv run pytest

  build-and-push:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v6

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=
            type=raw,value=latest

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push chess-reporter-engine-instance
        uses: docker/build-push-action@v7
        with:
          context: .
          file: docker/chess-reporter-engine-instance.Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/nielsencampos/chess-reporter-engine-instance:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push chess-reporter-engine-master
        uses: docker/build-push-action@v7
        with:
          context: .
          file: docker/chess-reporter-engine-master.Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/nielsencampos/chess-reporter-engine-master:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push chess-reporter-jupyter-lab
        uses: docker/build-push-action@v7
        with:
          context: .
          file: docker/chess-reporter-jupyter-lab.Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/nielsencampos/chess-reporter-jupyter-lab:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
